el = document.querySelector(".infinity-zen__container");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".banner-slider__content");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".desk-notif-card__card");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".ph-project-promo-container");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".ya-recommendation-widget");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".amb__picture");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".tb-rb-banner-wrapper");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".App-rightBottomRbBanner");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".AdvContainer-container");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".b-banner__content");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".ya-recommendation-widget-visible");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".pulse-lenta");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".grid__rcol");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.getElementById("yandex_ad_R-*");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}
el = document.querySelector(".styles-stickyWrap-cS9yM");
if (el!=null) {
    el_p = el.parentElement;
    el_p.removeChild(el);
}

